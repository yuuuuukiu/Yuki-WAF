# 生产环境 Nginx 反代配置（后续使用）
# 将 /api 反代到后端，/ 反代到前端静态资源
#
# upstream backend_api {
#     server backend:8000;
# }
#
# server {
#     listen 80;
#     server_name your-domain.com;
#
#     location /api/ {
#         proxy_pass http://backend_api;
#     }
#
#     location / {
#         root /usr/share/nginx/html;
#         try_files $uri $uri/ /index.html;
#     }
# }
